import 'package:healthtrack1/user_profile.dart';

class HomeController {
  List<UserProfile> profiles = [
    UserProfile(name: 'John', age: 30, height: 180, weight: 75),
    // Добавьте другие профили по желанию
  ];

// Дополнительные методы для управления данными, например:
// void addUserProfile(UserProfile userProfile) {
//   profiles.add(userProfile);
// }
}
